apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: glm
  namespace: higress-system
spec:
  endpoints:
  - address: mock.mock.svc.cluster.local
    weight: 100
  hosts:
  - glm-cluster.local
  location: MESH_INTERNAL
  ports:
  - name: http
    number: 30002
    protocol: HTTP
  resolution: DNS
