apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: mirrors-traffic
  namespace: higress-system
spec:
  endpoints: []
  hosts:
  - mirrors-traffic-cluster.local
  location: MESH_INTERNAL
  ports:
  - name: http
    number: 30002
    protocol: HTTP
  resolution: DNS
