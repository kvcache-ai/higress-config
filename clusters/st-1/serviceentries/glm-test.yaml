apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: glm-test
  namespace: higress-system
spec:
  endpoints:
  - address: glm-test.default.svc.cluster.local
    weight: 100
  hosts:
  - glm-test-cluster.local
  location: MESH_INTERNAL
  ports:
  - name: http
    number: 30002
    protocol: HTTP
  resolution: DNS
