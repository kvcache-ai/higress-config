apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: night-traffic
  namespace: higress-system
spec:
  endpoints: []
  hosts:
  - night-traffic-cluster.local
  location: MESH_INTERNAL
  ports:
  - name: http
    number: 30002
    protocol: HTTP
  resolution: DNS
