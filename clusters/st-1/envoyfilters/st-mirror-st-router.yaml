apiVersion: networking.istio.io/v1alpha3
kind: EnvoyFilter
metadata:
  name: st-mirror-st-router
  namespace: higress-system
spec:
  configPatches:
  - applyTo: HTTP_ROUTE
    match:
      context: GATEWAY
      routeConfiguration:
        vhost:
          route:
            name: st-mirror-st
    patch:
      operation: MERGE
      value:
        match:
          case_sensitive: true
          prefix: /
        name: st-mirror-st
        route:
          cluster: outbound|30002||st-mirror-st.local
          timeout: 3600s
  priority: 100
  workloadSelector:
    labels:
      app: higress-gateway
